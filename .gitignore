# ── Claude Code scratch (per `~/.claude/CLAUDE.md` scratch-location convention) ──
.claude/

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ── Python virtual envs ──
.venv/
venv/
env/
ENV/

# ── Python testing + coverage ──
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/
.mypy_cache/
.ruff_cache/

# ── uv ──
.uv-cache/

# ── Node ──
node_modules/
.npm
.pnpm-store/
.yarn/
yarn-error.log
npm-debug.log*
.next/
.nuxt/
dist/

# ── Rust ──
target/
Cargo.lock.bak

# ── Editors + IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# ── OS general ──
ehthumbs.db
Thumbs.db

# ── Logs + tmp ──
*.log
*.tmp
*.pid
tmp/

# ── Secrets — NEVER commit ──
.env
.env.*
!.env.example
*.pem
*.key
*.token
oauth_token*
secrets/

# ── Build artifacts ──
*.bak
*.orig
